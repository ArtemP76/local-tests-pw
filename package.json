{
  "name": "local-tests-pw",
  "version": "1.0.0",
  "description": "Playwright e-to-e tests for localcoding.us",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:debug": "playwright test --headed",
    "report": "playwright show-report ",
    "format": "prettier ./ --write --list-different",
    "lint": "prettier ./ --check"
  },
  "author": "Artem",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "1.37.1",
    "dotenv": "16.3.1"
  },
  "devDependencies": {
    "prettier": "3.0.3"
  }
}
